// Stub — full plan editor is a separate planning pass.

export function Plans() {
  return (
    <div className="p-10">
      <h1 className="text-3xl font-extrabold mb-2">Plans</h1>
      <p className="text-gray-dark">
        Plan editor coming in the next pass. For now the app ships with two
        default plans you can run from Home.
      </p>
    </div>
  );
}
