# Build output
dist/
.vite/

# Vendored MediaPipe runtime assets (~44MB) — reproducible via
# `npm run vendor:mediapipe` (also runs automatically before dev/build), so
# they aren't committed. See scripts/vendor-mediapipe.mjs.
/public/models/wasm/
/public/models/*.task

# Dependencies
node_modules/

# Local data
saves/
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Claude local state
.claude/worktrees/
.claude/settings.local.json
